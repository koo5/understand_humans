
	/*
	reinterpret_as_hierarchical_notes = function(kb, doc_uri)
	{
		rewrite type
		add parameters,
		add children

	}
	*/

	/*
	reinterpret_as_hierarchical_notes = function(kb, doc_uri)
	{
		rewrite type
		add parameters,
		add children

	}
	*/

